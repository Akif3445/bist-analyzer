# BIST Smart Investment Assistant - Dependencies
# pip install -r requirements.txt

streamlit>=1.28.0
streamlit-option-menu>=0.3.6
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.40
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
feedparser>=6.0.0
borsapy>=0.10.0   # KAP bildirimleri (news_engine._src_kap) — import'u lazy, RAM'e dokunmuyor
python-dotenv>=1.0.0   # lokal .env okuma (ANTHROPIC_API_KEY, TURSO_*) — yoksa sessizce atlanır

# Note: transformers + torch are NOT included here.
# Streamlit Cloud free tier has ~1GB RAM — torch alone is 2GB+.
# The app auto-falls back to keyword-based sentiment when BERT is unavailable.
# To enable BERT locally: pip install transformers torch
